A=int(input("Escribe la Altura: "))
bama=int(input("Escribe la base menor: "))
espacios=0

for i in range (A):
    espacios=A-i
    print(" "*espacios+"* "*(i+1))
for i in range (A-3): 
    espacios=espacios+1
    print(" "*espacios+'* '*bama)
    bama=bama-1
